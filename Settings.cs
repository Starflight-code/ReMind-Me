namespace reMind_me {
    internal class Settings {
        // fast mode disables animations
        public bool fastMode;
        public Settings(bool fastMode) {
            this.fastMode = fastMode;
        }
    }
}
